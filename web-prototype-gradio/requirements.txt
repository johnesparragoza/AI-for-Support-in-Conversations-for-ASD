# ZeroGPU stack. The model path is kept identical to the working Streamlit
# Space: transformers 4.40.2 is what makes MC-LLaVA-3b's remote code find
# past_key_values.seen_tokens (removed in 4.41+), so KV caching works.
# torch is bumped to 2.8.0 because ZeroGPU only supports torch >=2.8.
spaces                 # HF ZeroGPU runtime (provides the @spaces.GPU decorator)
# gradio itself is installed by HF from README `sdk_version` (5.27.0) — pinning it
# here too causes version conflicts. 5.27.0 carries the fix for the gradio_client
# get_api_info() "argument of type 'bool' is not iterable" crash (gradio PR #11085)
# that gr.Image() triggered on 4.44.0.
torch==2.8.0
transformers==4.40.2
tokenizers==0.19.1
accelerate==0.30.1
numpy==1.26.4
sentencepiece==0.2.0
einops==0.7.0
pillow==10.3.0
gtts
