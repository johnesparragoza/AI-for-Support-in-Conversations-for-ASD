streamlit>=1.58.0
# Pin to the stack MC-LLaVA-3b's remote code was built against. transformers 4.5x+
# entangles AutoProcessor with mistral_common (not installed), and 5.0 dropped APIs
# the model needs. 4.45.2 is the author's stated floor and imports AutoProcessor cleanly.
transformers==4.45.2
tokenizers>=0.20,<0.21
torch
torchvision
accelerate
sentencepiece
einops
pillow
gtts
